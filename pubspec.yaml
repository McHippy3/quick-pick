name: flutter_quick_pick
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.0+1
  cupertino_icons: ^0.1.2
  location: ^1.4.1
  path_provider: ^0.5.0+1
  image_picker: ^0.4.12+1

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
  assets:
    - "assets/not_finished.jpg"
    - "assets/no-image.png"
  fonts:
  - family:  custom_icons
    fonts:
      - asset: fonts/CustomIcons.ttf
